# Aerie.Api's JSON console formatter (see Program.cs, enabled outside
# Development) writes one JSON object per log line: Timestamp, EventId,
# LogLevel, Category, Message, State, Exception.
[PARSER]
    Name    dotnet_json
    Format  json
